import { readInputLines } from '../shared/utils';

const part1 = (data: any): number => 0;

// const part2 = (data: Set<number>): number => 0;

(async () => {
    const input = await readInputLines('day1');

    console.log(part1(input));
    // console.log(part2(input));
})();